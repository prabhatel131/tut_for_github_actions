name: first workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a sample GitHub action
        run: echo "first workflow runs successfully"

  
